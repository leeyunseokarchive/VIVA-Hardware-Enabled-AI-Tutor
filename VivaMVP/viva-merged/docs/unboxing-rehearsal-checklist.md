# 언박싱 리허설 체크리스트 — WiFi 프로비저닝 P1/P2 실기기 검증

> 수치·문구 출처: `pi-server/provision.py`(BOOT_GRACE_S=30초, RUNTIME_LOSS_S=180초,
> POLL_S=5초, TOUCH_ZONE_Y_MIN=350), `pi-server/eyes.py`(P1/P2 문구, "재연결 시도 중…").
> **소스 상수를 바꾸면 이 문서도 같이 갱신할 것.**

- 수행일: ____ / 수행자: ____ / 로봇 IP: ____ / 커밋: ____

## 0. 배경과 범위

- **08-12 이미 검증됨** (process.md §4 8주차 항목 18): QR 인식 7.2초(15~25cm, AF
  continuous macro), nmcli 재등록, viva-server 와 카메라 공유 무충돌. 단 전부
  **SSH 세션에서 강제 실행한 결과**라, 진짜 미연결 상태의 화면 발동과 systemd
  경로의 nmcli 는 미검증.
- **이번에 보는 것** (§2 8주차 표 7번 잔여 = 회의 TODO ①): polkit 설치 후
  systemd 경로 연결, 진짜 미연결 P1/P2 화면, 터치 존, 임계값 체감.
- **회의 TODO ⑦(WiFi 재접속 n회+안내 폴백)은 삭제되고 이 리허설로 흡수됐다**
  — 현 구조(5초 폴링 무한 재시도 + 180초 후 P2 안내)가 요구를 이미 충족하는지를
  시나리오 B 에서 확인하는 것이 ⑦의 검증이다.

## 1. 사전 준비 — 전부 체크 후 시작

- [ ] 최신 pi-server 배포: `provision.py`(08-12 try_connect 수정 포함)·`eyes.py`+
      `fonts/` 디렉토리·`wake.py` — `fonts/` 가 빠지면 문구 없이 아이콘만 뜬다
- [ ] polkit 설치: Pi 에서 `sudo bash ~/pi-server/setup/install-polkit.sh` 실행,
      마지막 줄 **"전체 PASS"** 확인. FAIL 이면 리허설 중단하고 원인부터
- [ ] **복구 수단 확보** — 저장 WiFi 를 지우면 SSH 가 끊긴다(08-12 항목 13 교훈:
      미연결 고착 시 원격 복구 불가). 다음 중 하나 이상: ① USB 키보드(콘솔 로그인)
      ② SD 카드 편집 수단 ③ QR 스캔 실패 시 대비한 예비 핫스팟
- [ ] 현재 SSID/비밀번호 메모: ____ / 저장 연결 목록 기록:
      `nmcli -t -f NAME,TYPE connection show` → ____
- [ ] SSH 창에 로그 띄우기(연결이 살아있는 동안): `journalctl -u viva-provision -f`
- [ ] 스톱워치(폰 타이머) 준비 — 아래 측정값은 전부 초 단위 실측

## 2. 시나리오 A — 완전 미연결 (P1, 첫 언박싱 재현)

| # | 절차·확인 항목 | pass/fail | 측정값 |
|---|---|---|---|
| A1 | 저장 WiFi 전부 삭제: `sudo nmcli connection delete "<이름>"` (wifi 타입 전부) 후 `sudo reboot` | [ ] | - |
| A2 | 부팅 완료 후 **P1 이 유예 없이 즉시** 뜬다 (저장 네트워크 0개면 30초 유예 미적용 — decide_screen 로직) | [ ] | 부팅→P1 ____초 |
| A3 | P1 문구 3줄 정확: "VIVA 앱에 표시된 / QR 코드를 보여주세요 / 카메라는 헤드 하단에 있습니다" + QR 브래킷 아이콘. 아이콘만 뜨면 fonts/ 미배포 (`journalctl -u viva-eyes` 에 "한글 폰트 없음") | [ ] | - |
| A4 | 앱 VIVA for App/Device 홈 설정 버튼 → WifiProvisionScreen → QR 표시. QR 셀 사이 hairline(흰 줄) 없음 (QrCodeView 분수 셀 확인 — §1 잔여 항목) | [ ] | - |
| A5 | QR 을 헤드 하단 카메라 15~25cm 에 제시 → 인식 (journalctl "QR ok: <ssid>") | [ ] | 제시→인식 ____초 (기준 7.2초) |
| A6 | "connected" 로그 후 **로봇 화면이 P1→일반 눈으로 복귀** (5초 폴링이라 최대 ~5초 지연 정상) | [ ] | 인식→복귀 ____초 |
| A7 | 앱 화면이 성공(연결 완료)으로 전환 — 미연결→연결 엣지 판정(08-12 수정분) | [ ] | - |
| A8 | QR 표시부터 로봇 화면 복귀까지 총 소요 | [ ] | 총 ____초 |
| A9 | SSH 재접속 가능 + `curl http://<로봇IP>:5000/health` 응답 | [ ] | - |

## 3. 시나리오 B — 저장 WiFi 실패 (P2)

> 유도 팁: 집 공유기 대신 **폰 핫스팟을 A 에서 등록해 두고 핫스팟만 끄면**
> 공유기를 안 건드리고 안전하게 재현할 수 있다.

### B-1. 부팅 경로 (BOOT_GRACE_S = 30초)

핫스팟(저장 네트워크)을 끈 상태로 `sudo reboot`.

| # | 확인 항목 | pass/fail | 측정값 |
|---|---|---|---|
| B1-1 | 부팅 후 30초까지는 프로비저닝 화면이 **안 뜬다** (유예 동작) | [ ] | - |
| B1-2 | 약 30초 후 P2 표시 | [ ] | 부팅→P2 ____초 |
| B1-3 | P2 문구 3줄 정확: "WiFi 연결에 실패했어요 / VIVA 앱의 디바이스 설정 / 가이드에 따라 QR 코드를 스캔해주세요" | [ ] | - |
| B1-4 | 재연결 버튼(하단 알약 외곽선) + 원 최하단 반투명 "재연결 시도 중…" 상태 문구 표시 | [ ] | - |

**임계값 체감 기록 (BOOT_GRACE_S=30초):** [ ] 적당 [ ] 길다 [ ] 짧다 — 이유: ____

### B-2. 운영 중 끊김 경로 (RUNTIME_LOSS_S = 180초)

연결 정상 상태(일반 눈)에서 핫스팟/공유기 전원 차단, 스톱워치 시작.

| # | 확인 항목 | pass/fail | 측정값 |
|---|---|---|---|
| B2-1 | 차단 후 3분까지 로봇에 프로비저닝 화면이 **안 뜬다** (짧은 끊김/공유기 재부팅 오탐 방지). 주의: 앱·로봇의 `disconnected` 화면(폰+슬래시)은 별개 로직이라 먼저 뜰 수 있음 — 정상 | [ ] | - |
| B2-2 | 약 180초 후 P2 표시 | [ ] | 차단→P2 ____초 |

**임계값 체감 기록 (RUNTIME_LOSS_S=180초):** [ ] 적당 [ ] 길다 [ ] 짧다 — 이유: ____

### B-3. 터치 재시도 (= 회의 TODO ⑦ 검증)

핫스팟/공유기 전원을 다시 켠 직후 진행.

| # | 확인 항목 | pass/fail | 측정값 |
|---|---|---|---|
| B3-1 | P2 화면 **하단**(재연결 버튼 부근 — 터치 존은 화면 세로 350px 아래, 대략 아래쪽 1/4 전체) 탭 → journalctl 에 `[viva-provision] retry tapped` | [ ] | - |
| B3-2 | 화면 **중앙/상단** 탭은 무반응 (터치 존 y≥350 경계 확인) | [ ] | - |
| B3-3 | 재시도 후 저장 프로파일로 재연결 → 화면 복귀 | [ ] | 탭→복귀 ____초 |
| B3-4 | (⑦ 무한 재시도 확인) 터치 없이 방치해도 5초 폴링 + NM 자동연결로 저절로 복귀하는가 — 한 번은 터치 없이 재현해 볼 것 | [ ] | 방치→복귀 ____초 |

### B-4. P2 에서 QR 재등록 (공유기 비번 변경 시나리오 — systemd 경로 핵심 검증)

틀린 비번 유도: `sudo nmcli connection modify "<SSID>" 802-11-wireless-security.psk "wrongpass"` 후 재부팅 (SSH 가능할 때 미리 실행).

| # | 확인 항목 | pass/fail | 측정값 |
|---|---|---|---|
| B4-1 | 30초 후 P2 → 앱에서 올바른 QR 표시 → 스캔 → journalctl `QR ok` → `connected` (**connection modify+up 경로**가 polkit 설치 후 systemd 서비스에서 성공하는지 — 08-12 에 SSH 로만 통과했던 부분) | [ ] | 스캔→연결 ____초 |
| B4-2 | 실패 시 journalctl 에 `connect failed (비번 오류?)` 가 찍히고 P2 유지 (조용한 실패 없음) | [ ] | - |

## 4. 종합 판정

- 전 항목 pass → process.md §2 8주차 표 7번 잔여 소거 + 회의 TODO ①·⑦ 종결.
  결과·측정값을 §4 히스토리에 문제/해결/결과 서술로 기록(문서 갱신 규칙).
- fail 항목은 증상·journalctl 발췌와 함께 §4 `막힌 것` 에 기록.
- 리허설 종료 후 원상 복구: 핫스팟 프로파일 삭제, 원래 공유기 재등록, SSH 확인.
