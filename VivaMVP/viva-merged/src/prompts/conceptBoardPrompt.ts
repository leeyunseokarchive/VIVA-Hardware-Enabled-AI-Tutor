/** 개념 도해 전용 생성 규칙. 두 가지 사고를 막는다 (회의 피드백 2026-08-13):
 * ① 기존 board 프롬프트(문제 풀이 힌트용) 유용 시 물음표·질문 문구 혼입.
 * ② "화이트보드 판서" 문구를 모델이 문자 그대로 받아 칠판이라는 물체를
 *   그림 — 원하는 건 교과서 삽화처럼 앱 배경색 위에 바로 놓인 도해다.
 * 순수 모듈 — 런타임 폴백(board.service)과 사전생성 스크립트(ts-node) 공용.
 * 배경색 리터럴은 src/theme.ts APP_BACKGROUND_COLOR 와 동일해야 한다
 * (theme.ts 는 react-native import 라 여기서 못 가져온다 — 바꿀 땐 둘 다). */
export const CONCEPT_BOARD_RULES =
  '이 이미지는 교과서 삽화풍 개념 설명 도해다 - 문제나 퀴즈가 아니다.\n' +
  '- 캔버스 전체를 가장자리까지 정확히 #FAF7F0 단색으로 채운다. 그라데이션·질감·비네트·그림자·테두리 금지.\n' +
  '- 칠판·화이트보드·액자·나무 프레임·책상 같은 물체를 그리지 않는다 - 도해가 평평한 배경 위에 바로 놓인다.\n' +
  '- 또렷한 검정 선화 + 한글 용어 라벨. 강조가 필요한 곳에만 색 1~2개(각도 호, 면 음영 등)를 절제해 쓴다.\n' +
  '- 제목·헤더·캡션을 넣지 않는다 - 시키지 않은 글자를 지어내다 한글이 깨진다. 지시된 라벨과 수식만.\n' +
  '- 지시된 도해 외의 부속 그림(미니 썸네일, 장식 화살표, 의미 없는 각도 호)을 추가하지 않는다.\n' +
  '- 물음표(?)를 어디에도 그리지 않는다.\n' +
  '- 질문 문장, 빈칸 채우기, "구해 보자" 류 문구를 넣지 않는다.\n' +
  '- 문장 설명 금지 - 용어 라벨과 수식만.';

/** 사전생성 스크립트용 독립 프롬프트 — 개념 1개당 도해 1장.
 * sketch 는 conceptList 의 개념별 도해 지시문 — 뭘 그릴지는 모델 재량에
 * 맡기지 않는다(20개 품질 일관성). */
export function conceptBoardPrompt(name: string, sketch: string): string {
  return (
    `한국 중학생에게 "${name}" 개념을 설명하는 도해 이미지를 만들어라.\n` +
    `그릴 내용: ${sketch}\n` +
    '구성: 대표 도해 1개를 왼쪽에 크게, 핵심 정의·공식(2~4줄)을 오른쪽에. ' +
    '변형·전개 과정이 필요한 개념이면 화살표로 잇는 배치도 좋다.\n' +
    CONCEPT_BOARD_RULES
  );
}
