diff --git a/node_modules/react-native-live-audio-stream/android/build.gradle b/node_modules/react-native-live-audio-stream/android/build.gradle
index de6cdf1..a0725f3 100644
--- a/node_modules/react-native-live-audio-stream/android/build.gradle
+++ b/node_modules/react-native-live-audio-stream/android/build.gradle
@@ -10,6 +10,7 @@ def safeExtGet(prop, fallback) {
 }
 
 android {
+    namespace "com.imxiqi.rnliveaudiostream"
     compileSdkVersion safeExtGet("compileSdkVersion", DEFAULT_COMPILE_SDK_VERSION)
     buildToolsVersion safeExtGet("buildToolsVersion", DEFAULT_BUILD_TOOLS_VERSION)
 
diff --git a/node_modules/react-native-live-audio-stream/android/src/main/AndroidManifest.xml b/node_modules/react-native-live-audio-stream/android/src/main/AndroidManifest.xml
index e96278e..4d9a2f0 100644
--- a/node_modules/react-native-live-audio-stream/android/src/main/AndroidManifest.xml
+++ b/node_modules/react-native-live-audio-stream/android/src/main/AndroidManifest.xml
@@ -1,5 +1,4 @@
 
-<manifest xmlns:android="http://schemas.android.com/apk/res/android"
-          package="com.imxiqi.rnliveaudiostream">
+<manifest xmlns:android="http://schemas.android.com/apk/res/android">
     <uses-permission android:name="android.permission.RECORD_AUDIO"/>
 </manifest>
diff --git a/node_modules/react-native-live-audio-stream/ios/RNLiveAudioStream.m b/node_modules/react-native-live-audio-stream/ios/RNLiveAudioStream.m
index a6f2317..8883002 100644
--- a/node_modules/react-native-live-audio-stream/ios/RNLiveAudioStream.m
+++ b/node_modules/react-native-live-audio-stream/ios/RNLiveAudioStream.m
@@ -27,16 +27,26 @@ @implementation RNLiveAudioStream
 
     // Apple recommended:
     // Instead of setting your category and mode properties independently, set them at the same time
+    //
+    // VIVA patch (see patches/react-native-live-audio-stream+1.1.1.patch):
+    // upstream used AVAudioSessionModeVoiceChat, which is call mode - its
+    // default output route is the EARPIECE, and nothing in the app ever set
+    // the mode back to Default (expo-av only touches the category). So after
+    // the first STT session, all TTS playback came out of the earpiece at
+    // phone-call volume. We do turn-based (half-duplex) tutoring, not a
+    // call: no echo cancellation needed, so use Default mode and force the
+    // speaker route while the record category is active.
     if (@available(iOS 10.0, *)) {
         success = [audioSession setCategory: AVAudioSessionCategoryPlayAndRecord
-                                       mode: AVAudioSessionModeVoiceChat
-                                    options: AVAudioSessionCategoryOptionDuckOthers |
+                                       mode: AVAudioSessionModeDefault
+                                    options: AVAudioSessionCategoryOptionDefaultToSpeaker |
+                                             AVAudioSessionCategoryOptionDuckOthers |
                                              AVAudioSessionCategoryOptionAllowBluetooth |
                                              AVAudioSessionCategoryOptionAllowAirPlay
                                       error: &error];
     } else {
-        success = [audioSession setCategory: AVAudioSessionCategoryPlayAndRecord withOptions: AVAudioSessionCategoryOptionDuckOthers error: &error];
-        success = [audioSession setMode: AVAudioSessionModeVoiceChat error: &error] && success;
+        success = [audioSession setCategory: AVAudioSessionCategoryPlayAndRecord withOptions: AVAudioSessionCategoryOptionDefaultToSpeaker | AVAudioSessionCategoryOptionDuckOthers error: &error];
+        success = [audioSession setMode: AVAudioSessionModeDefault error: &error] && success;
     }
     if (!success || error != nil) {
         RCTLog(@"[RNLiveAudioStream] Problem setting up AVAudioSession category and mode. Error: %@", error);
